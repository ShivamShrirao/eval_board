services:
  web:
    build:
      context: .
      target: runner
    image: eval-board:web
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/eval_board
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8080
      NEXT_PUBLIC_S3_PUBLIC_BASE: ""
      AWS_SDK_LOAD_CONFIG: "1"
      IMAGE_CACHE_DIR: /opt/dlami/nvme/eval-board-cache
    volumes:
      - ${HOME}/.aws:/root/.aws:ro
      - /opt/dlami/nvme/eval-board-cache:/opt/dlami/nvme/eval-board-cache
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: eval_board
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d eval_board"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
